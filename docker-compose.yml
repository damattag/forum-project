services:
  postgres:
    container_name: forum-project-postgres
    image: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: forum-project
      PGDATA: /data/postgres
    volumes:
      - ./data/pg:/data/postgres
    